#include "30_Inventory.h"


bool Inventory::UseItem(int itemIndex)
{
    // potion 이 아닐경우 예외 처리 하는게 좋지만
    // 궂이 요구 하지 않았으니 귀찮으니 스킵한다.

    /*switch (items[itemIndex].itemName)
    {
        case HP_POTION:
            {
                cout << "* " << items[itemIndex].itemName << " used!";
                //cout << "HP restored: by " <<  <<"( -> " << " )\n"
            }
        break;
        case MP_POTION:
            {
                cout << "* " << items[itemIndex].itemName << " used!";
            }
        break;
    }
    */
    
    
    
    
    
    return true;
}
